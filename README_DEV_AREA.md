Área Dev (cadastros) — implementada em `Untitled-1.html`.

O cadastro de novos usuários agora registra um item em `state.devSignups` e a página `Área Dev` deve exibir essa lista.

Observação: este projeto é 100% front-end (localStorage). Todos os registros ficam apenas no navegador.

